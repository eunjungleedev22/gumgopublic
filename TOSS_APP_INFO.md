# 토스 앱인토스(Apps in Toss) 등록 정보

`apps-in-toss.toss.im` 콘솔의 "앱 정보 등록하기 > 기본 정보" 화면에 그대로 붙여넣을 수 있도록
정리한 등록용 텍스트입니다. 실제 콘솔의 글자수 제한(한국어 앱 이름 10자, 영어 앱 이름 15자,
부제 20자 등)에 맞춰뒀습니다.

## 기본 정보

| 필드 | 값 | 비고 |
|---|---|---|
| 한국어 앱 이름 | 통장통통 | 4/10자 |
| 영어 앱 이름 | ChubbyWallet | 12/15자 |
| 부제 | 지갑은 두껍게, 뱃살은 얇게 | 15/20자 |
| appName | `meogeuncheok` | 등록 후 수정 불가. `app.json`의 `expo.slug`/`expo.scheme`과 동일하게 맞춤 |
| 사용 연령 | 전체 이용가 | 폭력·선정성 등 제한 콘텐츠 없음 |

### 상세 설명 (500자 이내)

> 배고플 때 이성을 잃고 배달을 시키는 순간을 막는 습관형 절약 앱. ①오늘 먹고 싶은 음식·양·가격 입력 ②로컬 DB 기반 칼로리·탄단백지방 실시간 추정 ③냄새→한입→씹기→삼킴→포만감 순서의 섭취 연출로 실제로 안 먹고도 심리적 포만감 유도 ④결과 화면에서 절약 금액·회피 칼로리 즉시 확인 ⑤히스토리에서 일별·주간 누적 통계 확인. 참을 때마다 절약액이 눈에 보여 충동 소비와 과다 섭취를 행동 전에 예방하고, 배고픈 순간마다 다시 실행하게 되는 반복 사용 습관을 만든다.

약 265/500자. 문제 정의 → 유저 저니(①~⑤) → 기대 효과 순서로 구성했습니다.

## 고객문의 정보

| 필드 | 값 |
|---|---|
| 이메일 주소 | jessica.techie@gmail.com |

계정 이메일을 임시로 넣었습니다. 별도의 고객문의 전용 메일을 쓰실 계획이면 교체해 주세요.

## 카테고리 및 노출

### 카테고리

콘솔에서 "게임/비게임" 중 선택하는 항목이 있는데, 이 앱은 **비게임**을 선택하면 됩니다.
대분류/중분류 카테고리는 실제 콘솔 드롭다운에서 "생활" 계열 중 가장 가까운 항목(건강,
가계부·재테크 등)으로 선택하는 것을 추천하며, 정확한 카테고리 명칭은 콘솔에서 직접 확인이
필요합니다.

### 앱 검색 키워드

사용자가 이 앱을 떠올릴 만한 상황·목적 위주로 골랐습니다. 앞쪽일수록 우선순위가 높습니다.

```
식비절약, 배달비절약, 충동구매방지, 다이어트, 칼로리계산, 절약, 가계부,
소비습관, 야식참기, 배달음식, 식단관리, 무지출챌린지, 저축, 다이어트기록
```

### 업로드 에셋

모두 `store-assets/` 폴더에 있고, 콘솔 규격(정확한 픽셀 크기, PNG, 투명 배경 없음,
둥근 모서리 없음)에 맞춰 생성했습니다. 스크린샷은 실제 앱을 로컬에서 구동해 캡처한
화면이며, 표시된 수치는 앱의 음식 DB 값과 일치하는 예시 데이터입니다.

| 항목 | 파일 | 규격 |
|---|---|---|
| 앱 로고 | `store-assets/logo-light-600.png` | 600 × 600 |
| 다크모드 앱 로고 | `store-assets/logo-dark-600.png` | 600 × 600 |
| 스크린샷 세로형 ① | `store-assets/screenshot-portrait-1.png` | 636 × 1048 |
| 스크린샷 세로형 ② | `store-assets/screenshot-portrait-2.png` | 636 × 1048 |
| 스크린샷 세로형 ③ | `store-assets/screenshot-portrait-3.png` | 636 × 1048 |
| 스크린샷 세로형 ④ | `store-assets/screenshot-portrait-4.png` | 636 × 1048 |
| 스크린샷 세로형 ⑤ | `store-assets/screenshot-portrait-5.png` | 636 × 1048 |
| 스크린샷 가로형 ① | `store-assets/screenshot-landscape-1.png` | 1504 × 741 |

세로형은 최소 3장이 필요한데, 먼저 제출한 순서대로 미니앱 홈에 노출된다고 해서
핵심 흐름(홈 → 입력 → 최면 → 결과 → 통계) 순서로 5장을 준비했습니다.

각 스크린샷의 헤드라인:

1. `01 — 절약` / 참을 때마다 **통장이** 두꺼워져요 — 홈 대시보드
2. `02 — 기록` / 음식·양·가격만 넣으면 **자동 계산** — 기록 입력
3. `03 — 참기` / 먹는 상상으로 식욕을 흘려보내요 — 가상 섭취 연출 (피치 바탕)
4. `04 — 보상` / 안 먹은 만큼 **바로 쌓여요** — 결과
5. `05 — 습관` / 일별·주간 통계로 흐름을 봐요 — 히스토리

**로고 컨셉**: 태그라인을 그대로 도형화한 마크 — 두꺼운 바 위, 얇은 바 아래
(지갑은 두껍게, 뱃살은 얇게). 도형이 둘뿐이라 48px까지 줄여도 형태가 유지되고,
일러스트 스타일이 없어서 유행을 타지 않습니다. 라이트는 버터(#FBE7BF) 바탕에
적색 마크, 다크는 잉크(#2A2622) 바탕에 버터 마크로 같은 형태를 유지합니다.

## 디자인 시스템

스토어 에셋만 예쁘게 만들어도 스크린샷이 곧 앱 화면이라 소용이 없어서, 앱 UI 자체를
다시 잡았습니다. 어두운 면을 한 장도 쓰지 않는 라이트 팔레트입니다.

| 항목 | 값 |
|---|---|
| 배경 | `#F7F4EC` (크림) |
| 표면 | `#FFFFFF` — 톤 대신 그림자로 분리 |
| 입력 히어로 | `#FBE7BF` (버터) + 테두리 `#F0DCB0` |
| 최면 화면 | `#FBDDCE` (피치) — 이 화면에서만 바탕색이 바뀝니다 |
| 강조 (절약 금액·CTA) | `#A8352A` (적색) |
| 칼로리 | `#7A5230` (브론즈) |
| 탄·단·지 | `#D9A441` / `#5A7A9B` / `#C4724F` |

원칙 네 가지:

1. **진입 1순위는 입력** — 화면을 열었을 때 할 일은 "먹고 싶은 걸 적는 것" 하나뿐이라,
   누적 금액이 아니라 입력 카드가 상단을 차지합니다. 누적은 CTA 아래 한 줄짜리
   스트립으로 내리고 연속 참은 일수와 묶었습니다.
2. **화면에서 가장 진한 것은 CTA 하나** — 어두운 블록을 전부 걷어내서 적색 버튼이
   유일한 고대비 요소가 되게 했습니다. 무게가 아니라 면적과 여백으로 위계를 만듭니다.
3. **색상 계열은 하나** — 크림·버터·피치가 전부 같은 난색 축이라 색끼리 다투지
   않습니다. 이전 청록 히어로는 크림 바탕의 보색이라 명도를 아무리 낮춰도 면이
   진동했습니다.
4. **모든 조합 WCAG AA** — 본문 4.5:1, 인접 면 3:1 기준으로 16개 조합을 계산해
   전부 통과시켰습니다. 플레이스홀더는 `#6F6558`(5.71:1), 리스트 보조 텍스트는
   `#7D7567`(4.55:1)까지 올렸습니다.

스토어 슬라이드는 앱과 같은 팔레트를 순서대로 밟습니다 — 크림 → 버터 → 피치 →
적색 → 크림. 보상 슬라이드만 적색 바탕으로 반전시켜서 시리즈가 가장 센 면에서
끝나게 했습니다.

## 사용자 식별

`@apps-in-toss/web-framework`의 `User.getAnonymousKey()`로 받은 `hash`를 사용자
식별자로 씁니다. 기록은 이 식별자별로 저장돼요.

토스 로그인(`userKey`)은 쓰지 않습니다. 토스의 마이그레이션 가이드가 안내하는
최종 상태가 "hash를 표준 식별자로 쓰고 토스 로그인 의존성을 제거"인데, 이 앱은
신규라 이전할 기존 `userKey` 사용자가 없어서 처음부터 그 상태로 시작하면 됩니다.
덕분에 서버, 계약, 복호화 키, 개인정보 동의 항목이 모두 필요 없어요.

구현 시 주의한 점:

- **v3 이름을 씁니다.** 마이그레이션 가이드의 `getUserKeyForGame()`은 v3에서
  deprecated된 v2 별칭이고, 가이드가 분기하라는 `'INVALID_CATEGORY'`는 v3
  타입에 존재하지 않습니다. v3 시그니처는
  `Promise<{ type: 'HASH'; hash: string } | 'ERROR' | undefined>`예요.
- **SDK는 지연 로딩합니다.** 토스 웹뷰 밖(현재 웹 배포)에서는 SDK가 동작하지
  않으므로 `await import(...)`로 부르고, 실패·`'ERROR'`·구버전(`undefined`)은
  모두 로컬 식별자로 폴백해 앱이 그대로 동작합니다.
- **기존 기록을 잃지 않습니다.** 식별자 도입 전 키(`meogeuncheok:entries:v1`)에
  남아 있던 기록은 처음 여는 식별자가 승계하고, 원본 키는 지우지 않습니다.

토스 로그인이 필요해지는 시점은 기기 교체 후 기록 승계, 기능성 푸시·알림,
프로모션, 토스페이를 쓸 때입니다. 그때는 서버가 필요해요 — 클라이언트는
`TossAuth.login()`으로 `authorizationCode`까지만 받고, AccessToken 교환과 사용자
정보 복호화는 서버에서 해야 하며 복호화 키는 클라이언트에 둘 수 없습니다.

## 에셋 재생성 방법

디자인이나 앱 화면이 바뀌면 아래 순서로 다시 만들면 됩니다. (Noto Sans CJK KR 폰트와
Playwright Chromium 필요)

```bash
npx expo export -p web --output-dir dist   # 정적 빌드
node scripts/store-assets/server.js &      # cleanUrls 지원 로컬 서버 (8099)
node scripts/store-assets/capture.js       # 앱 화면 5종 캡처
node scripts/store-assets/make-logo.js      # 로고 라이트/다크
node scripts/store-assets/make-app-icons.js# 앱 아이콘 (assets/images)
node scripts/store-assets/make-portrait.js # 세로형 5장
node scripts/store-assets/make-landscape.js# 가로형 1장
```
